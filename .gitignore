# Claude workspace files (not part of the website)
.claude/memory/
.claude/plans/
.claude/skills/
*.jsonl

# Videos in root brand_assets/ are tracked (damhorsevid.mp4, landscapevid.mp4)
# .mov/.MOV excluded — originals kept locally but not needed on GitHub
brand_assets/*.mov
brand_assets/*.MOV
brand_assets/*.avi
brand_assets/*.webm
# .claude/brand_assets/ is a defunct duplicate — exclude everything in it
.claude/brand_assets/

# Dev-only screenshot folder
temporary screenshots/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Node
node_modules/
