# Claude workspace files (not part of the website)
.claude/memory/
.claude/plans/
.claude/skills/
*.jsonl

# Video files — large files excluded until ready to push
# landscapevid.MOV (177 MB) excluded until converted to .mp4
brand_assets/*.mov
brand_assets/*.MOV
brand_assets/*.avi
brand_assets/*.webm
.claude/brand_assets/*.mp4
.claude/brand_assets/*.mov
.claude/brand_assets/*.MOV

# Dev-only screenshot folder
temporary screenshots/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Node
node_modules/
